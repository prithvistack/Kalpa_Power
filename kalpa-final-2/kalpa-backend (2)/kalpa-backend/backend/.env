# PostgreSQL (active)
DATABASE_URL=postgresql://postgres:Swarajya%4019@localhost:5432/kalpa_db
# SQLite rollback — change DATABASE_URL back to this line to revert instantly
# DATABASE_URL=sqlite:///./kalpa.db
# Generated for dev — replace with output of:
#   python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=kalpa-dev-secret-2024-please-change-before-production-deploy
JWT_ALGORITHM=HS256
TOKEN_EXPIRE_MINUTES=60
GOOGLE_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com
# Comma-separated list of allowed frontend origins
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:3000
# Default admin account — legacy keys kept for backward compatibility
DEFAULT_ADMIN_EMAIL=admin@kalpa.com
DEFAULT_ADMIN_PASSWORD=Kalpa@Admin2024
DEFAULT_ADMIN_NAME=System Administrator

# Phase 3.8 — permanent seeded administrator (takes precedence over DEFAULT_ADMIN_*)
# Seeding is idempotent: only creates/upgrades this specific email, never duplicates
SEEDED_ADMIN_EMAIL=admin@kalpa.com
SEEDED_ADMIN_PASSWORD=Kalpa@Admin2024

# Environment — set to "production" to disable dev endpoints and demo seed
APP_ENV=development

# Gmail SMTP — Google App Password (not Gmail login password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=prithvikhedekar9092@gmail.com
SMTP_PASSWORD=cpjqykpgcirjuobg
SMTP_FROM=prithvikhedekar9092@gmail.com

# MFA settings
OTP_EXPIRE_MINUTES=5
OTP_MAX_ATTEMPTS=5
